// Deprecated compatibility shim
// The auth store implementation was moved to `authStore.ts`.
// Keep this file minimal so TypeScript/IDE tooling doesn't fail if any imports still reference `./stores/auth`.

export {}
